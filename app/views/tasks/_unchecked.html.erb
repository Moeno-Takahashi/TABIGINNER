<%= link_to user_task_path(task.user_tasks.find { |b| b.user_id == current_user.id }), id: "js-user-tasks-#{task.id}", method: :delete,  remote: true do %>
<i class="fas fa-check-circle"></i>
<% end %>